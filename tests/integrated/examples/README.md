Examples Run
============

This test checks that all the cases in the `examples` directory can be
run. It doesn't check that they can run to completion; only that they
can be started and be evolved a very short time-step. This ensures
that they are updated whenever there are breaking changes to the
input options.
